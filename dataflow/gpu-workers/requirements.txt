Pillow==8.0.1
apache-beam[gcp]==2.26.0  # Make sure this matches with the boot file copy in the Dockerfile.
rasterio==1.1.8
tensorflow==2.4.0  # Make sure this matches the Dockerfile base image tensorflow version.
